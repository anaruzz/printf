#include "holberton.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
  int a=10;
int* ptr =&a;
printf("%p\n", (void *)ptr);
_printf("%p\n", (void *)ptr);
_printf("Can you print an address?\n%p\nNice!\n", (void *)0x7fff5100b6f8);
    return (0);
}
